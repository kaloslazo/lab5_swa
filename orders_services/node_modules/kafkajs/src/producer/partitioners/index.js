const DefaultPartitioner = require('kafkajs/src/producer/partitioners/default')
const LegacyPartitioner = require('kafkajs/src/producer/partitioners/legacy')

module.exports = {
  DefaultPartitioner,
  LegacyPartitioner,
  /**
   * @deprecated Use DefaultPartitioner instead
   *
   * The JavaCompatiblePartitioner was renamed DefaultPartitioner
   * and made to be the default in 2.0.0.
   */
  JavaCompatiblePartitioner: DefaultPartitioner,
}
