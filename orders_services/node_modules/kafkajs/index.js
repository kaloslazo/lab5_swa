const Kafka = require('kafkajs/src')
const PartitionAssigners = require('kafkajs/src/consumer/assigners')
const AssignerProtocol = require('kafkajs/src/consumer/assignerProtocol')
const Partitioners = require('kafkajs/src/producer/partitioners')
const Compression = require('kafkajs/src/protocol/message/compression')
const ConfigResourceTypes = require('kafkajs/src/protocol/configResourceTypes')
const ConfigSource = require('kafkajs/src/protocol/configSource')
const AclResourceTypes = require('kafkajs/src/protocol/aclResourceTypes')
const AclOperationTypes = require('kafkajs/src/protocol/aclOperationTypes')
const AclPermissionTypes = require('kafkajs/src/protocol/aclPermissionTypes')
const ResourcePatternTypes = require('kafkajs/src/protocol/resourcePatternTypes')
const { isRebalancing, isKafkaJSError, ...errors } = require('kafkajs/src/errors')
const { LEVELS } = require('kafkajs/src/loggers')

module.exports = {
  Kafka,
  PartitionAssigners,
  AssignerProtocol,
  Partitioners,
  logLevel: LEVELS,
  CompressionTypes: Compression.Types,
  CompressionCodecs: Compression.Codecs,
  ConfigResourceTypes,
  AclResourceTypes,
  AclOperationTypes,
  AclPermissionTypes,
  ResourcePatternTypes,
  ConfigSource,
  ...errors,
}
